name: build-and-package

on:
  push:
  pull_request:

jobs:
  linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scons

      - name: Build
        run: scons platform=linux target=template_release

      - name: Package addon artifact
        run: |
          mkdir -p dist/linux/addons/godot-kcp/bin
          cp godot-kcp.gdextension dist/linux/addons/godot-kcp/
          cp bin/libgodot-kcp*.so dist/linux/addons/godot-kcp/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/linux/**

  windows:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scons

      - name: Build
        run: scons platform=windows target=template_release arch=x86_64

      - name: Package addon artifact
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force -Path dist/windows/addons/godot-kcp/bin | Out-Null
          Copy-Item godot-kcp.gdextension dist/windows/addons/godot-kcp/
          Copy-Item bin\libgodot-kcp*.dll dist/windows/addons/godot-kcp/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/windows/**

  macos:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install scons

      - name: Build
        run: scons platform=macos target=template_release arch=arm64

      - name: Package addon artifact
        run: |
          mkdir -p dist/macos/addons/godot-kcp/bin
          cp godot-kcp.gdextension dist/macos/addons/godot-kcp/
          cp -R bin/libgodot-kcp*.framework dist/macos/addons/godot-kcp/bin/ || true
          cp bin/libgodot-kcp*.dylib dist/macos/addons/godot-kcp/bin/ || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/macos/**

  android:
    runs-on: ubuntu-latest
    env:
      ANDROID_NDK_VERSION: 23.1.7779620
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up NDK
        uses: android-actions/setup-ndk@v2
        with:
          ndk-version: ${{ env.ANDROID_NDK_VERSION }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scons

      - name: Build
        env:
          ANDROID_NDK_ROOT: ${{ env.ANDROID_NDK }}
        run: scons platform=android target=template_release arch=arm64v8

      - name: Package addon artifact
        run: |
          mkdir -p dist/android/addons/godot-kcp/bin
          cp godot-kcp.gdextension dist/android/addons/godot-kcp/
          cp bin/libgodot-kcp*.so dist/android/addons/godot-kcp/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/android/**

  ios:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install dependencies
        run: |
          python3 -m pip install --upgrade pip
          python3 -m pip install scons

      - name: Build
        run: scons platform=ios target=template_release arch=arm64 ios_simulator=no

      - name: Package addon artifact
        run: |
          mkdir -p dist/ios/addons/godot-kcp/bin
          cp godot-kcp.gdextension dist/ios/addons/godot-kcp/
          cp bin/libgodot-kcp*.a dist/ios/addons/godot-kcp/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/ios/**

  web:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Set up Emscripten
        uses: mymindstorm/setup-emsdk@v12
        with:
          version: 3.1.43
          actions-cache: true

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install scons

      - name: Build
        env:
          EMSDK: ${{ env.EMSDK }}
        run: scons platform=web target=template_release

      - name: Package addon artifact
        run: |
          mkdir -p dist/web/addons/godot-kcp/bin
          cp godot-kcp.gdextension dist/web/addons/godot-kcp/
          cp bin/libgodot-kcp*.wasm dist/web/addons/godot-kcp/bin/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: godot-kcp-all-platforms
          path: dist/web/**
